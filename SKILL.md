---
name: design-game-ui-kit
description: "Design game UI from actual gameplay and screenshots, then turn reviewed mockups into clean transparent sprites and Unity-ready asset kits. Use for 游戏UI设计、主页面/设置/弹窗示意图、确认后精细拆图、半身透明头像、UI素材分层与交付. Not for generic websites or code-only UI bug fixes."
---

# 游戏 UI 设计与精细素材交付

把真实玩法、视觉稿和可用游戏素材连接起来。示意图是设计依据，交付物应能独立拼装，而不是带着底色和邻近内容的矩形截图。

## 选择当前阶段

- 用户要求“先出图看看，之后再拆”：完成视觉示意图并展示，停在用户指定的设计评审节点。
- 用户已经确认“开始拆分”：直接进入素材制作，不重新请求已获得的确认。
- 用户只要头像/Logo等：只处理指定资产；不要扩展成整套页面。
- 用户要求完整流程且没有设定评审节点：按现有信息推进；重要风格选择可在制作中提出非阻塞澄清。
- 修改 Unity 项目、接业务逻辑、制作演示视频各自依照实际授权。设计与导出资源本身不表示可以替换现有场景或游戏素材。

## 1. 先理解游戏

有项目路径时，优先检查玩法规则、输入行为、UI控制器、布局脚本、相关场景/Prefab、人物和字体/本地化资源。不要遍历无关SDK或配置秘密。

识别：玩家核心循环、页面功能、按钮语义、动态内容、状态切换、横竖屏和适配约束。检查运行时布局是否会覆盖编辑器中的排序、尺寸和锚点。截图里的颜色占位块或不明图标不是可据此猜定的功能。

先形成简短的功能与图层清单。不要只看“房间”就把逻辑推理游戏设计成家居展示；不要把画叉操作与自动排除、候选与确认、重新开始与继续游戏混淆。

## 2. 做可评审的视觉稿

参考现有美术确定色彩、圆角、线条、阴影、字体层级和图标风格。保留用户指定功能；额外功能应少量且合理。主次操作、危险操作、可访问的开关状态要清楚。

使用当前可用的 imagegen 技能/工具制作需要AI绘制的位图，并遵守其工具约束。明确每张参考图的角色：构图、风格、身份或功能。既有矢量图标优先沿用可编辑母版。

按真实屏幕比例出整页稿，预留安全区域、长文本和本地化空间。多页面可并排展示；只有总稿时不声称已有分层源文件。保存生成提示词和图像到当前项目的版本化设计目录。

如用户要多个独立人物，逐个生成资产，统一头身比例、底部基线和边距；保留原人物身份特征。试稿中的新增角色应注明。人物半身之外透明，不带卡片、圆框或纸底。

## 3. 从确认稿制作素材

进入此阶段先读 [精细拆分与交付要求](references/asset-production.md)。

按用途拆：背景、Logo、主要插画、面板、无字按钮底、图标、开关轨道/滑块、选中/禁用/按下状态、头像/头像框、徽章、分割线。相同组件复用，不通过复制相同文件夸大素材数量。

所有交互控件与动态文字独立。完整装饰插画可以作为一个元素；如果用户要求内部照片、道具或动画层，也要继续拆，并补全被遮挡部分。不能把未分层的组合图称为所有内部元素已独立。

每项注明属于原像素提取、AI去字/补全、矢量重建或项目原资源。优先保持确认稿；需要重建时保持风格与语义并报告有意义的差异。不能用随手重画的简化组件冒充忠实切片，也不能用AI生成声称恢复了隐藏的原始像素。

## 4. 验证后才交付

逐项检查完整轮廓、孔洞、纸边、头发、透明内孔、阴影、浅色衣服、白色图标，以及画布外缘是否截断。生成器画出的棋盘格不是透明通道。

使用 `scripts/check_ui_assets.py` 检查素材清单并输出深浅底色验收图；运行方式和格式见参考文档。该脚本只读取资产、写报告与预览，不修改源素材。自动检查通过不等于抠图已干净，主代理必须查看验收图，必要时放大检查并修正实际问题。

用交付素材和独立文字重组目标页面，而不是再次展示原概念图。检查漏件、拉伸、布局溢出、按钮各状态和开关的组合。用户已给具体设备尺寸时以该尺寸验证。

Unity交付包含PNG、可选SVG母版、各边九宫格参数、布局坐标/锚点、文字与状态接线说明。只有实际运行过才声称已在Unity验证。图片检查、页面重组与运行验证分别报告。

打包可用资源、清单、导入说明和验收图；把原始生成图、中间失败图与临时处理文件留在包外。给用户素材包和重组预览的直接链接，说明本次完成与尚未接入的部分。

## 此用户的已完成示例

继续 Murdoku 项目或用户说“像上次那套”时，读 [Murdoku案例](references/murdoku-example.md)。它是项目上下文，不是其他游戏必须采用的画风或尺寸。
